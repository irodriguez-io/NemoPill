# Active Task Packet

## How To Use This File

- This file must contain exactly one active task.
- Update it before every handoff to a coding agent.
- The coding agent must treat this file as the only active task contract for planning and implementation.

## Task Status

| Field | Value |
| --- | --- |
| Milestone ID | `M-001` |
| Task ID | `T-007` |
| Task title | Wire Kover coverage instrumentation (per ADR-044) + Robolectric test-runner configuration + Roborazzi snapshot infrastructure + `unit-integration-ui-snapshot` and `coverage` CI stages; assess and route ADR-049 two Konsist rules; assemble M-001 milestone close evidence for all seven Done When items. |
| Requested by | Isidro Rodriguez (Human gatekeeper); closes M-001 reviewable scope per `_context/07 § Milestone Register M-001` Done When items (4) coverage tool generates a committable report and (7) ADRs appended at milestone close; items 1, 2, 3, 5, 6 satisfied at T-006 close. |
| Task status | `Approved for apply` |
| Date last updated | `2026-05-27` |

- `Ready for proposal` means this packet is authored and awaits Human gatekeeper review. The Project Manager role cannot advance `Task status` to `Approved for apply` per CLAUDE.md; only the Human gatekeeper may flip it after reviewing this packet in a separate conversation.
- **Standard three-conversation pattern applies at T-007 per ADR-064.** T-007 follows the canonical sequence: (i) this PM-role conversation produces the packet at `Ready for proposal`; (ii) a separate Human-gatekeeper-review conversation reviews this packet, optionally edits, and flips `Task status` to `Approved for apply`; (iii) a third conversation invokes execution against the approved packet.
- **T-007 is the second Apply-Mode-with-`_source/`-write task and the final task of M-001.** Its close assembles the M-001 milestone evidence package and routes to the Human gatekeeper for M-001 milestone sign-off, which opens M-002 reviewable scope.
- **Per ADR-075**, the Konsist package path is `io.nemopill.core.konsist` (not `com.nemopill.core.konsist` as incorrectly stated in the T-006 packet). This packet uses `io.nemopill.*` throughout, consistent with `_context/04`.

## Task Objective

- Problem to solve now: T-006 closed M-001 Done When items 1, 2, 3, 5, and 6. Two items remain open: (4) coverage tool generates a committable report, and (7) ADRs introduced during M-001 implementation appended to `_context/09_decision_log.md` before milestone close. Additionally, three infrastructure pieces deferred from T-006 — Kover plugin configuration, Robolectric test-runner wiring, and Roborazzi snapshot infrastructure — are the prerequisite scaffolding for all M-002+ tasks that write business code and tests. Without Kover wired now, the first Domain use case that lands in M-002 has no coverage gate; without Robolectric configured, the first Robolectric test in the suite cannot find its runner; without Roborazzi on the `:app` classpath, M-003's first Compose snapshot test requires a dependency-wiring task before any snapshot can be taken. T-007 closes these gaps and closes M-001.
- Why now: T-006 complete per the 2026-05-25 Developer handoff entry → the QA and Human gatekeeper review window for T-006 is open; T-007 is the natural follow-on task that finishes M-001. Deferring past T-007 would require the Developer role to interrupt the first M-002 business-code task to wire infrastructure that is more reviewable on its own, separate from any business logic.
- Expected user-visible or system-visible outcome: After T-007 execution lands and merges, `./gradlew koverVerify` runs on all six modules and exits zero with per-module coverage thresholds active (vacuously satisfied at T-007 because all `src/main/kotlin/` directories are still empty — the thresholds become meaningful the moment first business code lands in M-002). `./gradlew test` runs the full test suite including Konsist and any Robolectric tests wired by T-007, and exits zero. The GitHub Actions CI workflow gains two new stages: `unit-integration-ui-snapshot` (runs `./gradlew test`) and `coverage` (runs `./gradlew koverVerify` and uploads the HTML report as a CI artifact). Roborazzi is present on the `:app` test classpath so M-003's first `captureRoboImage()` call compiles without needing a separate dependency task. A T-007-close handoff entry assembles the M-001 Done When evidence package (all seven items) and routes to the Human gatekeeper for M-001 milestone sign-off and the M-002 PM refresh.

## In Scope

- **Wire Kover plugin across all six modules per ADR-044 and `_context/12 § Continuous Integration coverage stage`.** The `kover` plugin alias (`org.jetbrains.kotlinx.kover` version `0.8.3`) is already in `_source/gradle/libs.versions.toml`. Apply the plugin in the root `build.gradle.kts` (with `apply false` to control application per-module) and in each of the six module `build.gradle.kts` files. Configure `koverReport` in root to aggregate a merged multi-module HTML report at `_source/build/reports/kover/html/`. Configure per-module coverage thresholds in each feature module's `build.gradle.kts` via `koverVerify { rule { bound { minValue = <threshold> } } }`:
  - `:core`: ≥ 90 % line coverage on `io.nemopill.core.*` packages (pure-Kotlin shared kernel; vacuously true at T-007).
  - `:medication-management`, `:scheduling`, `:notifications`, `:adherence-tracking`: ≥ 90 % line on `*.domain.*` subpackages; ≥ 80 % line on `*.application.*` subpackages. No threshold on `*.infrastructure.*` or `*.presentation.*` per `_context/05 § Engineering Quality` coverage-thresholds decision and ADR-044.
  - `:app`: no percentage threshold (`:app` hosts Activities, DI entry points, and cross-cutting Compose screens — coverage is owned by integration and snapshot rows, not unit-line targets).
  - All thresholds pass vacuously at T-007 because production source sets are empty. The first business code to land in M-002 will be the first real threshold measurement.
- **Configure Robolectric in each Android module's `build.gradle.kts` per ADR-044 and `_context/12 § Continuous Integration unit-integration-ui-snapshot stage`.** `robolectric` (`org.robolectric:robolectric:4.14.1`) is already in `libs.versions.toml` under `[libraries]`. Add it as `testImplementation(libs.robolectric)` in: `:medication-management`, `:scheduling`, `:notifications`, `:adherence-tracking`, and `:app`. **`:core` is excluded** — it uses `kotlin("jvm")` (no Android Gradle Plugin), so `RobolectricTestRunner` cannot resolve Android SDK classes there and `testOptions.unitTests` is not valid DSL for a JVM module. For the Android library modules (`:medication-management`, `:scheduling`, `:notifications`, `:adherence-tracking`), configure the `testOptions { unitTests { includeAndroidResources = true } }` block in each library's `build.gradle.kts` so the Robolectric runtime can resolve Android resources. Add the same `testOptions` block to `:app::build.gradle.kts` (it is an Android application module). Add a minimal smoke-test class in `:app::src/test/kotlin/io/nemopill/app/` to verify the Robolectric runner resolves without error: one `@RunWith(RobolectricTestRunner::class)` JUnit4 test class with a single `@Test fun smoke_robolectric_runner_resolves() { assertTrue(true) }` assertion. This smoke-test is the `AC-003` positive-verification artifact; it is deleted (or replaced by real business tests) in M-002.
- **Wire Roborazzi snapshot infrastructure in `:app::build.gradle.kts` per ADR-044.** Add `roborazzi` and `roborazzi-compose` to `_source/gradle/libs.versions.toml` under a new `roborazzi` version entry (latest stable at execution time; `1.7.x` family as of the T-007 PM refresh — Developer role picks the exact version at execution time and records it in the T-007-close ADR). Add `testImplementation(libs.roborazzi)` and `testImplementation(libs.roborazzi.compose)` to `:app::build.gradle.kts`. Add a minimal smoke-test class in `:app::src/test/kotlin/io/nemopill/app/` confirming the `captureRoboImage` import resolves: `import io.github.takahirom.roborazzi.captureRoboImage` present, one empty `@Test fun smoke_roborazzi_import_resolves() { }`. No actual snapshot captures in T-007 — the first real `captureRoboImage()` call lands in M-003 when the first Compose screen is authored. The smoke-test is the `AC-004` positive-verification artifact and is deleted (or replaced) in M-003.
- **Add `unit-integration-ui-snapshot` CI stage to `.github/workflows/ci.yml`.** Stage 7 (job ID: `unit-integration-ui-snapshot`, display name `"7 · Unit / Integration / UI"`), runs after `build`, uses `ubuntu-latest`, requires `actions/setup-java`, `android-actions/setup-android@v3`, and `gradle/actions/setup-gradle` (cache read-only). Command: `./gradlew test`. Uploads test reports on failure: `_source/**/build/reports/tests/` as `test-reports` artifact, retention 7 days. This stage replaces the `arch-conformance` stage's narrow `:core:test` invocation with a full cross-module `./gradlew test` — Konsist assertions run again as part of the full suite, which is expected and desirable (two independent CI signals for architecture compliance: one fast/early from `arch-conformance`, one comprehensive/late from `unit-integration-ui-snapshot`).
- **Add `coverage` CI stage to `.github/workflows/ci.yml`.** Stage 8 (job ID: `coverage`, display name `"8 · Coverage"`), runs after `unit-integration-ui-snapshot`, uses `ubuntu-latest`. Command: `./gradlew koverVerify koverHtmlReport`. Uploads the HTML report as `kover-report` artifact (path: `_source/build/reports/kover/html/`, retention 14 days). An upload-artifact step runs unconditionally (not `if: failure()`) so the report is available for inspection on every PR regardless of pass/fail. This satisfies M-001 Done When item (4) "Coverage tool generates a committable report."
- **Assess ADR-049 two Konsist rules and route.** ADR-049 defines two rules deferred from T-006: (i) no `throw` statement in production code uses Kotlin string templates or `String.format` / `StringBuilder.append` in its message argument; (ii) every Domain-layer (`:domain`, `:core`) `data class` must override `toString()` to a redacted form. At T-007 execution time, verify whether any production `src/main/kotlin/` file contains a `throw` statement or a `data class`. At T-006 close, all source sets were empty; if they remain empty at T-007 execution, these rules remain deferred. The T-007 execution turn states the deferral routing explicitly in the T-007 handoff entry, naming the task slice that owns wiring each rule (the task that introduces the first `Result.Err.Unexpected` throw or the first sensitive Domain `data class` — expected to be an early M-002 task). If any production code has landed between T-006 and T-007 (unlikely given the standard three-conversation pattern), the execution turn wires the applicable rule(s) immediately.
- **Update the `branch protection` required status checks list to add the two new CI stages.** The branch protection rule on `main` was configured by the Human gatekeeper at T-006 close with six required checks (`setup`, `lint`, `secret-scan`, `framework-validate`, `arch-conformance`, `build`). T-007 adds two new CI jobs; the Human gatekeeper must add `unit-integration-ui-snapshot` and `coverage` to the branch protection rule's required checks at T-007 close. The Developer-role execution agent enumerates the two new job IDs in the T-007 handoff entry as a manual evidence step — same pattern as the T-006 branch-protection step.
- **Assemble M-001 milestone close evidence package.** T-007 is the final task of M-001. The T-007 execution close handoff entry enumerates evidence for all seven M-001 Done When items: (1) `assembleDebug` passes — APK path from T-006 CI run; (2) `./gradlew test` exits zero — T-007 CI run URL; (3) Konsist passes — T-006 CI run URL + T-007 `unit-integration-ui-snapshot` stage; (4) coverage tool generates committable report — T-007 `coverage` CI stage URL + Kover HTML report artifact link; (5) pre-commit hook blocks broken commit — T-006 demo transcript; (6) validator exits zero — confirmed at T-007 execution start; (7) ADRs appended — T-007-close ADRs (Kover config, Robolectric, Roborazzi) listed in file 09, plus confirmation that ADR-073/074/075 from T-006 are present. The close handoff also routes to: (a) Human gatekeeper for M-001 milestone sign-off (flip M-001 `Status: Planned → Active → Complete` in `_context/07`); (b) PM-refresh conversation for T-008 (the first M-002 task slice).
- **Append new ADRs to `_context/09_decision_log.md` at T-007 close per the post-T-003 amendment convention.** Expected ADRs T-007 surfaces: Kover 0.8.x per-module threshold configuration pattern (one ADR covering the threshold values and the vacuous-pass-until-code-lands convention); Robolectric 4.14.1 as the test runner with `includeAndroidResources = true`; Roborazzi version selected at execution time as the snapshot tool (ADR-044 already names it as the choice; this ADR records the concrete version and the `captureRoboImage` import path). If the ADR-049 routing decision (rules stay deferred because source sets are still empty) crystallizes as a durable convention ("infrastructure-only tasks whose source sets are still empty defer code-surface-dependent Konsist rules to the task that introduces the relevant code surface"), that convention is recorded as an ADR rather than left implicit.

## Explicitly Out Of Scope

- **All business code, Compose UI, use cases, adapters, Hilt wiring.** T-007 produces only test-infrastructure wiring; all `src/main/kotlin/` remain empty except for the two Robolectric and Roborazzi smoke-test classes (which are themselves deleted/replaced in M-002/M-003).
- **KSP plugin switch for Room and Hilt.** The T-006 handoff noted that Room and Hilt are currently wired as `annotationProcessor` in the feature module `build.gradle.kts` files. Switching to `ksp()` requires the `com.google.devtools.ksp` Gradle plugin and is most sensibly done when the first Room `@Database` or Hilt `@Module` is authored (M-002 or M-003). Wiring KSP now against empty source sets adds plugin resolution overhead with no functional benefit. Deferred to the first M-002 task that authors a Hilt adapter.
- **ADR-045 Room schema-JSON + Migration-object Konsist rules.** No `@Database` exists. Deferred to M-003 where the first Medication Management Room schema lands.
- **ADR-059 four `TokenLiteralRules.kt` Konsist rules.** No Compose code exists. Deferred to M-003 where the first `:medication-management::presentation` Compose screen is authored.
- **Roborazzi snapshot baseline captures.** No Compose screen to snapshot yet. Roborazzi is wired as an import-resolved classpath dependency; first `captureRoboImage()` call + baseline commit happens in M-003.
- **M-002 deliverables** — walking skeleton, Hilt DI graph, AlarmManager adapter, `BroadcastReceiver`, first Room schema, first Compose screen, manual Doze hardware test. T-007 scope ends at M-001 Done When item (7).
- **Gradle wrapper JAR.** `_source/gradle/wrapper/gradle-wrapper.jar` is a binary not generated by the Developer role. The gap is documented in the T-006 close handoff. The resolution path (run `cd _source && gradle wrapper --gradle-version 8.10.2` on a machine with Gradle installed, or rely on `gradle/actions/setup-gradle` downloading it for CI) remains unchanged. T-007 does not close this gap — it is tracked as a developer-onboarding note in the T-007 handoff entry.
- **Editing any `_context/` file other than `_context/08_active_task_packet.md` (this file), `_context/09_decision_log.md`, and `_context/handoff_log.md` at T-007 close.** `_context/07_delivery_plan_and_milestones.md` M-001 status flip (`Planned → Active` and then `Active → Complete`) is a Human-gatekeeper action at M-001 sign-off, not a Developer-role edit.

## Acceptance Criteria

- `AC-001` — `./gradlew koverVerify` exits zero on a clean baseline from all six modules. All per-module thresholds configured and vacuously satisfied. `./gradlew koverHtmlReport` produces an HTML report at `_source/build/reports/kover/html/index.html`. The coverage stage in the T-007 CI run uploads this report as the `kover-report` artifact. Satisfies M-001 Done When item (4).
- `AC-002` — `unit-integration-ui-snapshot` CI stage runs and passes on the T-007 implementation PR. `./gradlew test` runs across all six modules, including Konsist assertions (which pass per T-006 baseline) and the two smoke-test classes introduced by T-007. All jobs green. Satisfies M-001 Done When item (2) at T-007 scope (full cross-module test run, not just `:core:test`).
- `AC-003` — Robolectric smoke-test compiles and passes. The smoke-test class in `:app::src/test/kotlin/io/nemopill/app/SmokeRobolectricTest.kt` (or similar) annotated `@RunWith(RobolectricTestRunner::class)` compiles without error and its single `@Test fun smoke_robolectric_runner_resolves()` passes under `./gradlew :app:test`. No `AndroidSdk: ... not found` runtime errors. Note: `:core` is a `kotlin("jvm")` module and does not receive Robolectric; the smoke-test lives in `:app` where the Android runtime is available.
- `AC-004` — Roborazzi smoke-test compiles and the import resolves. The smoke-test class in `:app::src/test/kotlin/io/nemopill/app/SmokeRoborazziTest.kt` (or similar) with `import io.github.takahirom.roborazzi.captureRoboImage` compiles without error under `./gradlew :app:test`. No classpath-missing errors.
- `AC-005` — Two new CI stages (`unit-integration-ui-snapshot` and `coverage`) appear in the `.github/workflows/ci.yml` workflow with the correct `needs` dependency chain (`unit-integration-ui-snapshot` after `build`; `coverage` after `unit-integration-ui-snapshot`). Both stages pass on the T-007 implementation PR. The Human gatekeeper adds both new job IDs to the branch protection required checks at T-007 close; the T-007 handoff entry records this manual evidence step.
- `AC-006` — M-001 Done When items 1–7 all evidenced in the T-007 close handoff entry, following the evidence-assembly enumeration in `## In Scope`. Item (7) is satisfied when: (a) T-007-specific ADRs (Kover, Robolectric, Roborazzi) are prepended to `_context/09_decision_log.md`; (b) ADR-073, ADR-074, ADR-075 from T-006 are confirmed present; (c) any ADR-049 routing decision is recorded; (d) no M-001 ADR candidate from the T-006 or T-007 handoffs is left unrepresented. The handoff entry explicitly lists each ADR-ID with its title in the evidence package.
- `AC-007` — T-007-close handoff entry appended to `_context/handoff_log.md`. Entry covers: (i) all AC-001 through AC-006 evidence references; (ii) ADR-049 routing verdict (rules wired or deferred, with named target task); (iii) Gradle wrapper JAR gap acknowledgment (onboarding note, no resolution needed in T-007); (iv) branch protection required-checks addition (manual evidence step, complete or open status); (v) M-001 milestone close routing to Human gatekeeper; (vi) next role routing to PM for M-002 T-008 PM refresh; (vii) per-CLAUDE.md one-file-per-conversation closing line.

## Tests To Add First

- **T-007 is an infrastructure-only task with no business code and no file-03 BDD scenarios.** The convention established at T-006 applies: for infrastructure-only task packets that introduce no business logic, the `## Tests To Add First` section enumerates the infrastructure verification artifacts as the tests-first surface; the QA role's `Coverage Sufficient` / `Coverage Insufficient` verdict examines those artifacts rather than file-03 BDD scenarios.
- **`AC-001` Kover vacuous-pass is the tests-first artifact for coverage wiring.** The Kover `koverVerify` task passing with configured (but vacuously satisfied) thresholds demonstrates that the coverage gate is wired correctly and will fire when code lands. The verification: run `./gradlew koverVerify` locally on a clean baseline; observe exit-zero; inspect the HTML report to confirm per-module threshold blocks are present.
- **`AC-003` Robolectric smoke-test and `AC-004` Roborazzi smoke-test are the tests-first artifacts for test-runtime wiring.** Both classes exist in the tests-first pass; the Developer role writes them before verifying the full `./gradlew test` run succeeds. They are the "does the infrastructure actually work" gate before the CI stages are wired.
- **`AC-002` CI probe PR is the tests-first artifact for CI wiring.** The T-007 implementation PR itself is the probe; both new stages must be green before merge. QA's `Coverage Sufficient` verdict for T-007 requires all four of the above artifacts to be present and passing on the implementation PR.
- **No file-02 Business Rule (`BR-001`…`BR-012`) tests to write.** No business code introduced in T-007.
- **No file-03 BDD scenarios to wire.** No Patient-visible behavior introduced in T-007.

## Likely Change Surface

- **Files or modules likely added or affected:**
  - `_source/build.gradle.kts` (amended — Kover plugin added to root plugins block; `koverReport` aggregation configuration added).
  - `_source/gradle/libs.versions.toml` (amended — `roborazzi` version entry added under `[versions]`; `roborazzi` and `roborazzi-compose` library entries added under `[libraries]`).
  - `_source/core/build.gradle.kts` (amended — Kover plugin applied; **no Robolectric dependency and no `testOptions` block** — `:core` is `kotlin("jvm")` and the Android test runtime is not available there).
  - `_source/medication-management/build.gradle.kts`, `_source/scheduling/build.gradle.kts`, `_source/notifications/build.gradle.kts`, `_source/adherence-tracking/build.gradle.kts` (amended — Kover plugin applied; Robolectric `testImplementation` added; `testOptions.unitTests.includeAndroidResources = true` added; per-feature threshold rules added to `koverVerify` block).
  - `_source/app/build.gradle.kts` (amended — Kover plugin applied; Roborazzi `testImplementation` added; Robolectric `testImplementation` added; `testOptions.unitTests.includeAndroidResources = true` added).
  - `_source/app/src/test/kotlin/io/nemopill/app/SmokeRobolectricTest.kt` (created — Robolectric smoke-test class; deleted/replaced at M-002 when first real test lands).
  - `_source/app/src/test/kotlin/io/nemopill/app/SmokeRoborazziTest.kt` (created — Roborazzi smoke-test class; deleted/replaced at M-003 when first Compose snapshot test lands).
  - `.github/workflows/ci.yml` (amended — two new stages `unit-integration-ui-snapshot` and `coverage` appended; stage comment header updated to reflect T-007 scope).
  - `_context/09_decision_log.md` (amended — T-007 ADRs prepended per the post-T-003 amendment convention).
  - `_context/handoff_log.md` (amended — T-007-close handoff entry appended per `AC-007`).
  - `_context/08_active_task_packet.md` (this file — edited once during Human-gatekeeper-review conversation to flip `Task status` to `Approved for apply`; the Developer-role execution conversation reads but does not edit `Task status`; next PM refresh advances to T-008).
- **Files or modules that must remain untouched:**
  - All `_context/` files 01–07, 10–13. Per CLAUDE.md Core Rules, no `_context/` file other than 08, 09, and the handoff log is edited in T-007.
  - `_framework/` in its entirety (read-only template content).
  - `CLAUDE.md` (read-only instruction surface).
  - Existing Konsist rule files in `_source/core/src/test/kotlin/io/nemopill/core/konsist/` — T-007 does not amend Konsist rules.
  - `_build/hooks/pre-commit`, `.gitleaks.toml` — no hook or secret-scanner changes in T-007.
- **Contracts or schemas that may change:** the CI stage contract expands from 6 to 8 required stages. The branch protection rule's required checks list expands from 6 to 8. No runtime contracts change (no business code lands).

## Dependencies, Risks, And Blockers

- **Dependencies:**
  - T-006 complete per the 2026-05-25 Developer handoff entry: six-module Gradle scaffolding, Konsist suite, pre-commit hook, and six-stage CI workflow all in place and verified.
  - QA `Coverage Sufficient` verdict and Human gatekeeper flip to `Approved for apply` (standard three-conversation pattern per ADR-064).
  - Kover `0.8.3` and Robolectric `4.14.1` already cataloged in `libs.versions.toml` — no new version research needed for those two. Developer role selects Roborazzi version at execution time (latest stable `1.7.x`).
  - Branch protection rule access for Human gatekeeper to add two new required checks at T-007 close.
- **Risks:**
  - **Kover 0.8.x configuration-cache compatibility.** T-006 accepted the risk that `org.gradle.configuration-cache=true` (set in `gradle.properties`) could conflict with plugins; T-006 deferred Kover to avoid this risk. T-007 owns it. If Kover 0.8.x proves configuration-cache-incompatible, the execution agent records the finding in the T-007 handoff entry and toggles `org.gradle.configuration-cache=false` temporarily per the file-12 `DevOps Risks And Trade-Offs` empirical-tuning escape. Mitigation: test with `--configuration-cache` flag explicitly on a local build before landing.
  - **Roborazzi Compose dependency requiring Compose BOM.** Roborazzi's `roborazzi-compose` artifact depends on Compose UI; wiring it to `:app` without a Compose BOM declaration may cause version resolution conflicts. Mitigation: apply the existing `compose-bom` from `libs.versions.toml` as a BOM import on the `:app` test classpath, or limit the T-007 scope to `roborazzi` (core) only and defer `roborazzi-compose` to M-003 when the first Compose screen lands. Developer role resolves at execution time and records the choice as an ADR.
  - **Robolectric SDK jar download in CI.** Robolectric downloads Android SDK jars the first time a test runs. On a cold GitHub Actions runner this can add 30–60 seconds to the `unit-integration-ui-snapshot` stage. Mitigation: the `gradle/actions/setup-gradle` cache should include Robolectric's SDK jar cache after the first run. The risk is only the cold-start CI time, not correctness.
  - **ADR-049 rules still deferred.** If a contributor lands production code with a `throw` statement between T-006 close and T-007 execution (unlikely given the gatekeeper bandwidth), the rules may need emergency wiring. The Konsist suite currently has no rule guarding this — a violation silently passes until the rule is wired. Risk is low given the three-conversation gating pattern and the fact that all source sets are empty.
  - **Kover multi-module threshold vacuous-pass masking misconfiguration.** A Kover threshold block that is syntactically invalid but never fails (because coverage is 0/0 = vacuous pass) would silently let misconfiguration through. Mitigation: the `AC-001` acceptance criterion verifies that per-module threshold blocks are structurally present in the HTML report and that the `koverVerify` task explicitly lists the thresholds in its task description output (`./gradlew koverVerify --info` should enumerate each module's active rules).
- **Blockers: none.** T-006 artifacts are in place. T-007 execution prerequisites are a subset of T-006's (Java 17, Android SDK, gitleaks already verified at T-006 execution).

## Rollback Or Recovery Notes

- **Rollback:** if T-007 execution fails and the Human gatekeeper decides to abandon the produced state, rollback is `git restore _source/ .github/ _context/09_decision_log.md _context/handoff_log.md _context/08_active_task_packet.md`. No business logic to revert; the rollback eliminates only the coverage + test-runtime wiring.
- **Recovery from partial implementation:** same three-path pattern as T-006 — (a) resume in a new conversation reading the mid-session handoff entry; (b) full restart via `git restore`; (c) re-scope T-007 mid-execution if a discovered constraint forces descoping (e.g., Roborazzi Compose transitive dependency proves incompatible with the current Compose BOM baseline — in this case descope `roborazzi-compose` to M-003 via a mid-session handoff entry and re-route through PM refresh). Path (a) is preferred.
- **M-001 partial-close risk.** If T-007 produces the Kover + test-runtime artifacts but the CI stages fail for an unrelated environment reason, M-001 Done When item (4) is not satisfied (no committed CI artifact). The implementation PR stays open; the Human gatekeeper does not flip M-001 to Complete. Fix path: push a follow-up commit fixing the CI environment issue; no new PM-refresh conversation needed unless scope changes.

## Handoff Rules For The Agent

- **Proposal Mode is the default for T-007 authoring (this PM-role pass).** Apply Mode for T-007 execution requires the Human gatekeeper to flip `Task status` to `Approved for apply` in a separate gatekeeper-review conversation per ADR-064.
- **The execution agent must respect `io.nemopill.*` as the project-wide package namespace per ADR-075.** All new source files go under `io/nemopill/` paths. No `com.nemopill` anywhere in T-007 artifacts.
- **The execution agent must follow the tests-first discipline.** Smoke-test classes for Robolectric (`AC-003`) and Roborazzi (`AC-004`) are written and verified before declaring the infrastructure wiring complete. `./gradlew koverVerify` is run locally (or in a pre-merge branch build) before the CI stages are pushed.
- **The execution agent must assess the ADR-049 Konsist rules at execution time.** Check whether any production `src/main/kotlin/` file contains a `throw` statement or a Domain-layer `data class`. State the assessment and routing explicitly in the T-007 close handoff entry — "rules wired" or "rules deferred to T-### because source sets are still empty."
- **The execution agent must assemble the M-001 milestone close evidence package per `AC-006`.** This is the canonical T-007 close artifact that the Human gatekeeper reads to authorize M-001 sign-off. All seven Done When items must have concrete evidence references (CI run URL, artifact link, file path, or transcript).
- **The execution agent appends new ADRs directly to `_context/09_decision_log.md` at task close per the post-T-003 amendment convention.** Each new ADR follows the 4-field skeleton (Context, Decision, Status, Consequences), lands at the top of `## Current State`, newest-first. The T-007-close handoff entry enumerates ADR-IDs and titles. No ADR is queued for future backfill.
- **The execution agent must surface conflicts in handoff entries, not silently resolve them.** If a Kover or Roborazzi API constraint forces a substantive workaround, the workaround lands as an ADR at `Status: Proposed` pending Human ratification.
- **The branch protection required-checks addition is the manual evidence step for T-007** (analogous to T-006's branch-protection step). The execution agent enumerates the two new job IDs in the handoff entry; the Human gatekeeper applies them at the GitHub UI and the handoff records the completion or open status.
