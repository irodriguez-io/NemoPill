# Active Task Packet

## How To Use This File

- This file must contain exactly one active task.
- Update it before every handoff to a coding agent.
- The coding agent must treat this file as the only active task contract for planning and implementation.

## Task Status

| Field | Value |
| --- | --- |
| Milestone ID | `M-002` |
| Task ID | `T-008` |
| Task title | First M-002 walking-skeleton slice — the scheduling leg: a single "Schedule the Demo Reminder" button drives Compose → ViewModel → `ScheduleDemoReminderUseCase` → `SchedulerPort` → real `AlarmManagerSchedulerAdapter.setAlarmClock(now + 10 min)`. Stands up the Hilt + KSP DI graph (first `@Module`), the first `:core` shared-kernel production code (`Result`, `DoseId`, `ClockPort` + `SystemClockAdapter`), and the first `:scheduling` production code, proven by a use-case unit test and a Robolectric integration test that asserts the scheduling intent. |
| Requested by | Isidro Rodriguez (Human gatekeeper). M-001 signed off and flipped to `Complete` in `_context/07`; branch protection on `main` expanded to 8 required checks (`unit-integration-ui-snapshot` + `coverage` added). M-002 reviewable scope is open. This packet slices the first reviewable vertical of M-002 per `_context/07 § Milestone Register M-002` Done When items (1) and the Robolectric half of (2). |
| Task status | `Approved for apply` |
| Date last updated | `2026-06-03` |

- `Ready for proposal` means this packet is authored and awaits Human gatekeeper review. Per CLAUDE.md, the Project Manager role **cannot** advance `Task status` to `Approved for apply`; only the Human gatekeeper may flip it after QA renders `Coverage Sufficient` and Security renders `Guardrail Pass` (Security is activated for T-008 — see Handoff Rules).
- **Standard three-conversation pattern applies (ADR-064):** (i) this PM-role conversation produces the packet at `Ready for proposal`; (ii) a separate QA/Security + Human-gatekeeper review conversation renders verdicts, optionally edits, and flips `Task status` to `Approved for apply`; (iii) a third conversation invokes Developer-role execution against the approved packet.
- **Execution host (ADR-077):** T-008 is an Apply-Mode `_source/`-write task. Per the cross-environment operating model, Developer-role execution runs in **Claude Code opened at the repository root**, not in Cowork. The instructions in this packet are written to be directly executable there: exact module paths, package names, Gradle tasks, and acceptance commands are spelled out.
- **Package namespace is `io.nemopill.*` throughout (ADR-075).** Every new source file goes under `io/nemopill/` paths. No `com.nemopill` anywhere.

## Task Objective

- **Problem to solve now:** M-001 delivered the build, test, and quality-gate scaffolding but **zero production code** — every `src/main/kotlin/` is still empty. M-002's goal is to retire `_context/01` assumption (1) ("Android's local notification + scheduled-alarm APIs are reliable enough to fire Reminders on time when backgrounded or in Doze") via an end-to-end vertical slice. That full slice is too large for one reviewable task packet (3–4 hrs/week gatekeeper bandwidth → one task per packet, one approval cycle per `_context/07 § Task Slicing Rules`). T-008 takes the **first and load-bearing leg**: getting a hardcoded demo Dose's Reminder *scheduled* on the real `AlarmManager` via the full Clean-Architecture path (Compose → ViewModel → use case → `SchedulerPort` → `AlarmManagerSchedulerAdapter`). Everything downstream in M-002 (the Reminder firing into a notification, the inline "Mark Taken" action, the Room `Confirmation` write, the on-screen Adherence counter, boot re-arm) depends on a Reminder first being scheduled — so this is the natural first cut. It also forces the project's first Hilt `@Module` and the KSP plugin switch (carried forward from T-006), and turns the Kover thresholds (deferred at T-007 because source sets were empty) into real, measured gates.
- **Why now:** M-001 is `Complete` as of this session; the M-002 entry condition ("M-001 complete") is satisfied. Slicing the scheduling leg first lets the gatekeeper review a coherent, self-contained spine — the DI graph plus the schedule path — before the notification/persistence legs add their own surface area.
- **Expected user-visible / system-visible outcome:** After T-008 lands and merges, launching the debug APK shows a single screen with one button, **"Schedule the Demo Reminder."** Tapping it registers an `AlarmManager.setAlarmClock` for `now + 10 minutes` through the real `AlarmManagerSchedulerAdapter` and updates an on-screen line to confirm the scheduled time (e.g., "Reminder scheduled for 14:32:10"). No notification fires yet (that is the next slice). The Hilt graph compiles and wires `ClockPort`, `SchedulerPort`, the use case, and the `ViewModel`. `./gradlew :core:test :scheduling:test :app:test koverVerify` exits zero, including a new Robolectric integration test that fast-forwards `ClockPort` and asserts the scheduling intent. All eight CI stages stay green. A T-008-close handoff entry records the ADR-049 routing verdict and the new ADRs.

## In Scope

- **First `:core` shared-kernel production code** (package root `io.nemopill.core`, module is `kotlin("jvm")` — **no Android imports**):
  - `result/Result.kt` — the sealed `Result<out T, out E>` type from `_context/04 § Error handling` and `_context/05`. Two-tier per `_context/04`: an `Ok<T>` case and an `Err<E>` case, with a built-in `Err.Unexpected` variant (carrying a non-PII message — never Patient data per `_context/13`) for the unexpected-failure tier. Keep it minimal but final-shaped; later milestones extend the `Err` catalog, they do not redesign it.
  - `id/DoseId.kt` — `@JvmInline value class DoseId(val value: String)`. (Other ID newtypes — `MedicationId`, `DoseScheduleId`, `ConfirmationId` — are **out of scope**; add them when their owning feature lands.)
  - `port/ClockPort.kt` — `interface ClockPort { fun now(): Instant }` per `_context/04 § Ports`. `port/SystemClockAdapter.kt` — wraps `Clock.systemUTC()`, returns `Instant`. (`TimezonePort` / `SystemTimezoneAdapter` are **out of scope** for T-008 — the demo Dose uses a fixed `now + 10 min` offset and needs no `ZoneId` interpretation; add them with the first Dose-materialization slice.)
- **First `:scheduling` production code** (package root `io.nemopill.scheduling`):
  - `application/SchedulerPort.kt` — `interface SchedulerPort` exposing `suspend fun scheduleReminder(doseId: DoseId, at: Instant)`, `suspend fun cancel(doseId: DoseId)`, and `suspend fun cancelAll()`, per `_context/04 § SchedulerPort`. **Idempotent by `doseId`** — re-scheduling the same `doseId` replaces, not duplicates (BR-004 exactly-once; the adapter achieves this via a stable PendingIntent request code derived from `doseId`).
  - `application/ScheduleDemoReminderUseCase.kt` — the single demo use case: reads `now` from `ClockPort`, computes `now + DEMO_REMINDER_OFFSET`, calls `SchedulerPort.scheduleReminder(demoDoseId, target)`, returns `Result<Instant, Result.Err>` (the scheduled `Instant` on success). Catches adapter-boundary exceptions and maps to `Result.Err.Unexpected` per the `_context/04` two-tier policy. **This is the project's first Application use case** — follow the side-effect-isolation rule (all I/O via ports; the use case is `suspend`, the Domain stays pure).
  - `infrastructure/AlarmManagerSchedulerAdapter.kt` — implements `SchedulerPort` using `AlarmManager.setAlarmClock(AlarmManager.AlarmClockInfo(triggerAtMillis, showIntent), operation)`. The `operation` PendingIntent targets `ReminderAlarmReceiver` (see next bullet); the `showIntent` may target `MainActivity` or be null. **All PendingIntents constructed with `PendingIntent.FLAG_IMMUTABLE`** (`| FLAG_UPDATE_CURRENT` for idempotent replace) — enforced by the existing `PendingIntentFlagImmutableRule` Konsist rule. Request code is a stable function of `doseId.value` so re-scheduling is a no-op replace. Reads `AlarmManager` via injected `Context`/system-service (provided by Hilt).
  - `infrastructure/ReminderAlarmReceiver.kt` — **moved here from `:app`** to satisfy `_context/04 § :scheduling` ("Hosts … `BootCompletedReceiver`, `TimezoneChangedReceiver`" and the alarm receiver) and to let the adapter reference the receiver class without a `:scheduling → :app` dependency (illegal per the module graph). For T-008 the `onReceive` body stays a minimal no-op stub (logging only) — **notification rendering is the next slice (M-002 item 3), explicitly out of scope here.** The class must be `exported="false"`-compatible.
  - `SchedulingConstants.kt` — `internal const`/`val` home for `DEMO_REMINDER_OFFSET = Duration.ofMinutes(10)`, the hardcoded `demoDoseId`, and the PendingIntent request-code base. (The `WorkManager` backoff, `goAsync()` 8-second cap, and exact-alarm permission-flavor constants from `_context/12 § Scheduling runtime constants` are **out of scope** — no `WorkManager` and no async receiver work in T-008.)
- **First Hilt + KSP DI graph wiring in `:app`** (package root `io.nemopill.app`):
  - Add the **KSP** and **Hilt Gradle** plugins. They are absent from `_source/gradle/libs.versions.toml [plugins]` today (the Hilt *libraries* `hilt-android` / `hilt-compiler` are already cataloged). Add aliases `ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }` (new `ksp` version entry; pick the KSP build matching the catalog's Kotlin version at execution time — KSP versions are Kotlin-version-suffixed, e.g. `2.1.0-1.0.29`) and `hilt-android = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }` (reuse the existing `hilt = "2.51.1"` version). Declare both `apply false` in root `build.gradle.kts`; apply them in `:app` (and in any feature module that needs Hilt codegen — `:scheduling` if its adapter/use case use constructor `@Inject` that requires the module's own component, otherwise plain constructor `@Inject` needs only the `hilt-android` library on the classpath and codegen in `:app`; Developer confirms the minimal placement at execution time and records it as an ADR).
  - `NemoPillApplication` — annotate `@HiltAndroidApp` (currently a bare `Application` subclass).
  - `di/SchedulingModule.kt` (or `di/AppModule.kt`) — Hilt `@Module` binding `ClockPort` → `SystemClockAdapter`, `SchedulerPort` → `AlarmManagerSchedulerAdapter`, and providing the `AlarmManager` system service from the application `Context`. **This is the project's first `@Module`.**
  - `MainActivity` — annotate `@AndroidEntryPoint`; set Compose content to the demo screen.
  - `presentation/DemoViewModel.kt` — `@HiltViewModel`, injects `ScheduleDemoReminderUseCase`, exposes a `StateFlow<DemoUiState>` and a `schedule()` function that runs the use case in `viewModelScope` and reflects the `Result` in UI state. Keep `DemoUiState` minimal (idle / scheduled-at-`Instant` / error). **The Adherence counter and observing a `Confirmation` via `StateFlow` are M-002 item (5) — out of scope here.**
  - `presentation/DemoScreen.kt` — a single Compose screen: one "Schedule the Demo Reminder" button + a status line bound to `DemoUiState`.
  - Add catalog dependencies T-008 needs that are not yet present: `kotlinx-coroutines-core` and `kotlinx-coroutines-android` (ViewModel `viewModelScope`), `kotlinx-coroutines-test` (test), `truth` (assertions per `_context/04 § Testing toolchain`), and — if `hiltViewModel()` is used in Compose — `androidx-hilt-navigation-compose`. Wire `hilt-android` + `hilt-compiler` (KSP) into `:app` (and `:scheduling` if required).
- **Activate the deferred Kover per-package verify thresholds** for the modules that now receive production code, per the ADR-078 deferral ("wire when first production code lands"):
  - `:core` ≥ 90 % line on `io.nemopill.core.*`.
  - `:scheduling` ≥ 90 % line on `io.nemopill.scheduling.*.domain.*` (none yet — vacuous) and ≥ 80 % line on `io.nemopill.scheduling.*.application.*`. No threshold on `infrastructure.*` or `presentation.*`.
  - Use whatever Kover 0.8.3 Android DSL form actually works (the T-007 close noted per-package filtered rules could not be configured against empty source sets; with real code present, confirm the working DSL and record it). `:app` keeps no percentage threshold.
  - The new `:core` and `:scheduling::application` code must **meet** these thresholds — the use case and `Result`/`ClockPort` are unit-tested to the bar.
- **Update the `:app` manifest** to point the alarm `<receiver>` at the moved class `io.nemopill.scheduling.infrastructure.ReminderAlarmReceiver` (keep `exported="false"`), and **delete** the now-unused `io.nemopill.app.reminder.ReminderAlarmReceiver` stub. **No new `<uses-permission>` entries** — `USE_EXACT_ALARM`, `SCHEDULE_EXACT_ALARM` (maxSdkVersion handling per `_context/12`), `RECEIVE_BOOT_COMPLETED`, and `POST_NOTIFICATIONS` are already declared. (M-002 item (7): zero new permissions beyond those a BR justifies.)
- **Assess and route the two ADR-049 Konsist rules at execution time, per ADR-081.** Rule (i) — no string-template / `String.format` / `StringBuilder.append` in a production `throw` message argument. Rule (ii) — every Domain-layer `data class` overrides `toString()` to a redacted form. T-008 introduces the first production `throw` candidate (the use-case boundary catch → `Result.Err.Unexpected`, plus any Domain precondition `IllegalArgumentException`). **PM routing:** if a production `throw` lands in T-008, the Developer wires rule (i) now; otherwise record the deferral. Rule (ii)'s target is **the first task that introduces a sensitive Domain `data class`** — expected to be the M-002 Dose-materialization slice (Dose entity) or the M-003 Medication CRUD slice; `DoseId` is a `value class`, not a `data class`, so it does not trigger rule (ii). Name the concrete target task in the close handoff.
- **Append new ADRs to `_context/09_decision_log.md` at T-008 close** (post-T-003 amendment convention, newest-first at top of `## Current State`). Expected: KSP + Hilt Gradle plugin wiring and minimal placement; coroutines + Truth catalog additions; Kover 0.8.3 per-package threshold DSL now that code exists (resolving the ADR-078 deferral); the demo scheduling-adapter design (`setAlarmClock`, idempotent request-code-by-`doseId`, `FLAG_IMMUTABLE`); the `ReminderAlarmReceiver` relocation to `:scheduling::infrastructure`; and the ADR-049 routing verdict if it crystallizes as a durable rule.

## Explicitly Out Of Scope

- **The rest of the M-002 walking skeleton** — all deferred to subsequent M-002 slices (T-009+):
  - Item (2) **hardware** half: the manual real-device Doze test (the Robolectric half is in scope; the on-hardware confirmation is a later slice and a milestone-close gate).
  - Item (3): notification rendering via `NotificationManagerAdapter`, the manifest notification channel, and the "Mark Taken" inline action.
  - Item (4): the `BroadcastReceiver` dispatch to a Confirmation use case and the Room `Confirmation` write (no Room, no `NotificationPort`, no `ConfirmationRepository` in T-008).
  - Item (5): the on-screen Adherence counter and observing a `Confirmation` via `StateFlow`.
  - Item (6): `BootCompletedReceiver` re-arm on reboot (the `:app` boot-receiver stub stays as-is; its migration to `:scheduling` lands with item (6)).
- **All four feature Bounded Contexts' real domain rules** — no `Medication`, `Dose`, `DoseSchedule`, or `Confirmation` entities; no BR-001…BR-012 logic. The demo uses a single hardcoded `DoseId`.
- **Room / persistence of any kind**, the single `NemoPillDatabase`, and any DAO. No data is persisted in T-008.
- **`TimezonePort` / `SystemTimezoneAdapter`, `DomainEventPublisher` / `InProcessEventBus`** — added with the slices that first need them (Dose materialization / cross-context events).
- **`WorkManager` workers** (horizon extension, `pending → missed` sweep) and the `_context/12` scheduling runtime constants for them.
- **CRUD UI, navigation graph beyond the single demo screen, theming work** beyond what the demo screen needs.
- **Editing any `_context/` file other than `08` (this file), `09_decision_log.md`, and `handoff_log.md`.** `_context/07` M-002 status stays `Active` until the M-002 milestone-close sign-off (a future Human-gatekeeper action), not a T-008 edit.
- **`_framework/`, `CLAUDE.md`, existing Konsist rule files** (except adding the ADR-049 rule(s) only if a `throw` lands), `_build/hooks/`, `.gitleaks.toml`.

## Acceptance Criteria

- `AC-001` — `./gradlew :core:test :scheduling:test :app:test` exits zero with the new unit and integration tests present and passing.
- `AC-002` — **Use-case unit test passes.** `ScheduleDemoReminderUseCaseTest` (in `:scheduling` test source set) uses a fake `SchedulerPort` and a `FakeClock(initial: Instant)`; asserts the use case calls `scheduleReminder(demoDoseId, initial + 10 min)` exactly once, returns `Result.Ok` carrying that `Instant`, and that a second invocation is idempotent (still a single logical registration). Uses `runTest` + Truth.
- `AC-003` — **Robolectric integration test passes (M-002 item (1) + Robolectric half of item (2)).** An integration test under Robolectric instantiates the real `AlarmManagerSchedulerAdapter` against `ShadowAlarmManager`, drives it from the use case with a `FakeClock`, and asserts: exactly one alarm is scheduled; its trigger time equals `now + 10 min`; and the `operation` PendingIntent is immutable (`FLAG_IMMUTABLE`) and targets `io.nemopill.scheduling.infrastructure.ReminderAlarmReceiver`. No `AndroidSdk … not found` runtime errors (`@Config(sdk = [26])` or project default).
- `AC-004` — **Hilt graph compiles and the app builds.** `./gradlew :app:assembleDebug` succeeds. `NemoPillApplication` is `@HiltAndroidApp`; the `@Module` binds `ClockPort` → `SystemClockAdapter` and `SchedulerPort` → `AlarmManagerSchedulerAdapter` and provides `AlarmManager`; `MainActivity` is `@AndroidEntryPoint`; `DemoViewModel` is `@HiltViewModel` and injects `ScheduleDemoReminderUseCase`. No Hilt missing-binding errors at compile time.
- `AC-005` — **KSP + Hilt Gradle plugins wired.** Both plugins are in `libs.versions.toml [plugins]`, declared `apply false` in root, and applied in the module(s) that need them; Hilt annotation processing runs via KSP (the build invokes `kspDebugKotlin`/equivalent) with no errors.
- `AC-006` — **Kover thresholds active and met.** `./gradlew koverVerify koverHtmlReport` exits zero with real (non-vacuous) per-package rules for `:core` (≥ 90 %) and `:scheduling::application` (≥ 80 %) present in the report; the new production code meets them. This resolves the ADR-078 deferral.
- `AC-007` — **All eight CI stages green on the T-008 PR**, including `arch-conformance` (Konsist, with the `PendingIntentFlagImmutableRule` exercised by the new adapter), `unit-integration-ui-snapshot`, and `coverage`. The merged manifest declares **no new permissions** beyond those already present.
- `AC-008` — **ADR-049 routed and ADRs appended.** The close handoff states the ADR-049 verdict (rule (i) wired iff a production `throw` landed; rule (ii) deferred to a named target task). New ADRs (KSP/Hilt wiring, coroutines+Truth catalog additions, Kover per-package DSL, demo adapter design, receiver relocation) prepended to `_context/09_decision_log.md`, each with the 4-field skeleton (Context, Decision, Status, Consequences).
- `AC-009` — **T-008-close handoff entry appended** to `_context/handoff_log.md` covering: AC-001…AC-008 evidence references; the ADR-049 verdict and named target task(s); which M-002 Done When items this slice satisfied (1 and the Robolectric half of 2) and which remain open for T-009+; next-role routing to the PM for the T-009 slice; and the CLAUDE.md one-file-per-conversation closing line.

## Tests To Add First

- **`ScheduleDemoReminderUseCaseTest`** (`:scheduling/src/test/kotlin/io/nemopill/scheduling/application/`) — the Application-layer unit test for `AC-002`. Written before the use case is finalized. Fake `SchedulerPort` (records calls) + `FakeClock`. Asserts the offset math, the single idempotent registration, and the `Result.Ok` shape. `runTest` + Truth.
- **`AlarmManagerSchedulerAdapterRobolectricTest`** (`:scheduling/src/test/kotlin/io/nemopill/scheduling/infrastructure/`) — the integration test for `AC-003`, written before the adapter is finalized. Robolectric + `ShadowAlarmManager`. Asserts the scheduled trigger time, the `FLAG_IMMUTABLE` operation PendingIntent, and the receiver target. This is the canonical artifact that retires the Robolectric half of M-002 assumption (1) for the scheduling leg.
- **`FakeClock`** test fixture (`:scheduling` test source set for this slice; promote to `:core` `testFixtures` if a second module needs it later) — `ClockPort` fake with a settable `Instant`, per `_context/04 § ClockPort`.
- **`ResultTest`** (`:core/src/test/kotlin/io/nemopill/core/result/`) — light unit coverage of `Ok`/`Err`/`Err.Unexpected` so `:core` meets its ≥ 90 % threshold.
- **Optional light Compose render check** — a single Robolectric/Compose test that the demo screen renders the button. The full `DemoViewModel` state-flow behavior beyond schedule-confirmation is minimal in this slice; keep this test light or defer if it threatens slice size. No full snapshot baseline yet (Roborazzi baselines land in M-003).
- **No file-02 BR tests and no file-03 BDD scenarios** are wired in T-008 — no business rule and no Patient-facing journey is implemented; the demo is a hardcoded scheduling proof. The QA `Coverage Sufficient` verdict examines the use-case unit test, the Robolectric integration test, and the Kover threshold pass rather than file-03 BDD IDs.

## Likely Change Surface

- **Added / amended `_source` files:**
  - `gradle/libs.versions.toml` (amended — `ksp` version + `ksp`/`hilt-android` plugin aliases; `kotlinx-coroutines-core`/`-android`/`-test`, `truth`, optionally `androidx-hilt-navigation-compose` library entries).
  - `build.gradle.kts` (root — `ksp` + `hilt-android` plugins declared `apply false`).
  - `core/build.gradle.kts` (amended — Kover `:core` ≥ 90 % verify rule; add `kotlinx-coroutines-test` + `truth` test deps if `:core` gets tests needing them).
  - `core/src/main/kotlin/io/nemopill/core/result/Result.kt`, `id/DoseId.kt`, `port/ClockPort.kt`, `port/SystemClockAdapter.kt` (created — first `:core` production code).
  - `core/src/test/kotlin/io/nemopill/core/result/ResultTest.kt` (created).
  - `scheduling/build.gradle.kts` (amended — Hilt + KSP applied if needed; Hilt libs; coroutines; Truth; Kover `application` ≥ 80 % rule).
  - `scheduling/src/main/kotlin/io/nemopill/scheduling/application/SchedulerPort.kt`, `application/ScheduleDemoReminderUseCase.kt`, `infrastructure/AlarmManagerSchedulerAdapter.kt`, `infrastructure/ReminderAlarmReceiver.kt`, `SchedulingConstants.kt` (created/moved — first `:scheduling` production code).
  - `scheduling/src/test/kotlin/io/nemopill/scheduling/...` (created — use-case unit test, adapter Robolectric test, `FakeClock`).
  - `app/build.gradle.kts` (amended — Hilt + KSP applied; Hilt libs; coroutines; hilt-navigation-compose if used).
  - `app/src/main/kotlin/io/nemopill/app/NemoPillApplication.kt` (amended — `@HiltAndroidApp`), `MainActivity.kt` (amended — `@AndroidEntryPoint` + Compose content), `di/SchedulingModule.kt` (created), `presentation/DemoViewModel.kt` + `presentation/DemoScreen.kt` (created).
  - `app/src/main/kotlin/io/nemopill/app/reminder/ReminderAlarmReceiver.kt` (deleted — moved to `:scheduling`).
  - `app/src/main/AndroidManifest.xml` (amended — `<receiver>` name → scheduling FQCN; no new permissions).
- **Amended `_context` files:** `09_decision_log.md` (T-008 ADRs); `handoff_log.md` (close entry); `08_active_task_packet.md` (this file — edited once at gatekeeper review to flip `Task status` to `Approved for apply`; the Developer reads but does not edit `Task status`; the next PM refresh advances to T-009).
- **Must remain untouched:** all `_context/` files 01–07, 10–13; `_framework/`; `CLAUDE.md`; existing Konsist rule files (except adding ADR-049 rule(s) only if a `throw` lands); `_build/hooks/`, `.gitleaks.toml`.
- **Contract changes:** the `:app` DI graph gains its first bindings; the `:scheduling` module exposes its first port (`SchedulerPort`) and adapter; no runtime persistence or network contract changes (there is no network, by design).

## Dependencies, Risks, And Blockers

- **Dependencies:** M-001 complete (foundation, Konsist suite, 8-stage CI, branch protection — all in place as of this session). Hilt libraries already cataloged. QA `Coverage Sufficient` + Security `Guardrail Pass` + Human-gatekeeper flip to `Approved for apply` before execution (ADR-064).
- **Risks:**
  - **KSP ↔ Kotlin version pin.** KSP must match the catalog's Kotlin version exactly. A mismatch fails configuration. *Mitigation:* Developer selects the KSP build for the pinned Kotlin at execution time; record the exact version as an ADR. Watch for KSP-vs-configuration-cache interaction (T-007 flagged Kover/config-cache sensitivity); if it surfaces, follow the `_context/12` empirical-tuning escape (`org.gradle.configuration-cache=false` temporarily) and record it.
  - **Hilt codegen placement across modules.** Constructor `@Inject` in `:scheduling` consumed by a `@Module` in `:app` may or may not require the Hilt Gradle plugin + KSP in `:scheduling`. *Mitigation:* start with Hilt applied only in `:app`; if the build reports missing components for `:scheduling` injectables, add Hilt+KSP to `:scheduling`; record the final placement as an ADR.
  - **`setAlarmClock` under Robolectric.** `ShadowAlarmManager` models `setAlarmClock`; confirm the Robolectric version on the classpath records `AlarmClockInfo` triggers (it does for 4.14.1). *Mitigation:* if the shadow does not expose the trigger as expected, assert via `getNextScheduledAlarm()`/`getScheduledAlarms()` and the operation PendingIntent instead.
  - **Kover 0.8.3 per-package DSL on Android modules.** T-007 could not configure filtered rules against empty source sets. *Mitigation:* with real code present, confirm the working `koverVerify`/report-filter DSL; if 0.8.3 still cannot express a per-package filter for an Android module, apply a module-level threshold scoped to the `application` package via source-set layout and record the limitation as an ADR (do not silently drop the gate).
  - **Compose + Robolectric test flakiness.** Keep the optional Compose render test light; the load-bearing proofs are the use-case unit test and the adapter Robolectric test.
- **Blockers: none.** All prerequisites are in place.

## Rollback Or Recovery Notes

- **Rollback:** `git restore _source/ _context/09_decision_log.md _context/handoff_log.md _context/08_active_task_packet.md` (or revert the squash-merge commit). No persisted data and no schema to migrate — rollback removes only the scheduling-leg code, the DI wiring, and the catalog/plugin additions.
- **Recovery from partial implementation:** prefer resuming in a new Claude Code conversation from a mid-session handoff entry. Alternatively `git restore` and restart. If a discovered constraint forces descoping (e.g., Kover per-package filtering is impossible on the Android module), descope that single item via a mid-session handoff entry and route the descoped piece through a PM refresh — do not silently drop an acceptance criterion.
- **Partial-merge guard:** if the code lands but a CI stage fails for an environment reason, the PR stays open and M-002 does not advance; push a fix-forward commit. No new PM refresh is needed unless scope changes.

## Handoff Rules For The Agent

- **Proposal Mode is the default for this PM-role authoring pass.** Apply Mode for T-008 execution requires the Human gatekeeper to flip `Task status` to `Approved for apply` in a separate review conversation, after **QA** renders `Coverage Sufficient` and **Security** renders `Guardrail Pass`. **Security is activated for T-008** per `_context/07 § Proposal review owner` and `_context/05`: the task touches IPC boundaries (a `BroadcastReceiver`, `PendingIntent` construction → `FLAG_IMMUTABLE` rule) and the exact-alarm permission surface. Security must confirm: `FLAG_IMMUTABLE` on every PendingIntent; `ReminderAlarmReceiver` is `exported="false"`; no new manifest permissions; no `INTERNET`/network imports; no Patient data in any `Result.Err`/log message (`_context/13`).
- **Execute in Claude Code opened at the repository root (ADR-077).** Read `CLAUDE.md` and `_context/` 01–09 (plus 10–13) first. Operate against this packet as the only active task contract. Follow tests-first discipline: write `ScheduleDemoReminderUseCaseTest`, `AlarmManagerSchedulerAdapterRobolectricTest`, and `ResultTest` (and `FakeClock`) before finalizing the production code; run `./gradlew :core:test :scheduling:test :app:test koverVerify` locally before pushing the CI probe PR.
- **Respect `io.nemopill.*` (ADR-075)** for every new file. Honor the Clean-Architecture boundaries: I/O only in the Application layer via ports; Domain pure and non-suspending; no Android imports in `:core`; `:scheduling` must not import `:app` or any other feature module.
- **Assess ADR-049 at execution time and route explicitly** (ADR-081): wire rule (i) now if a production `throw` lands; name the target task for rule (ii) (first sensitive Domain `data class`) in the close handoff.
- **Append ADRs at close** (newest-first in `## Current State`, 4-field skeleton) and **surface conflicts in the handoff, do not silently resolve them** — substantive workarounds land as ADRs at `Status: Proposed` pending Human ratification.
- **Append the T-008-close handoff entry** per `AC-009`, then end the session per CLAUDE.md's one-file-per-conversation rule and route the next role to a PM refresh for the T-009 slice.
