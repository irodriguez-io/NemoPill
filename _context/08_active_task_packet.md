# Active Task Packet

## How To Use This File

- This file must contain exactly one active task.
- Update it before every handoff to a coding agent.
- The coding agent must treat this file as the only active task contract for planning and implementation.

## Task Status

| Field | Value |
| --- | --- |
| Milestone ID | `M-000` |
| Task ID | `T-005` |
| Task title | Contextualize `_context/13_threat_model_and_data_classification.md` — fill the framework template's nine `##` sections (Scope And Assumptions, Data Classification, Trust Boundaries, STRIDE Threat Table, Abuse Cases, Identity And Access Model, Logging And Audit Requirements, Privacy And Regulatory Mapping, Open Security Questions) with concrete threats, classifications, and controls grounded in the device-locked, no-`INTERNET` threat model NemoPill already commits to in `_context/05` and `_context/06`; ratify-or-supersede the plain-text Room encryption decision per ADR-043; close M-000 by reconciling the design-tail rows of `_context/07 § Dependency And Blocker Register` per ADR-039 |
| Requested by | Isidro Rodriguez (Human gatekeeper); satisfies `_context/07` M-000 Done When item (5) and closes M-000 itself (Done When item (8) the M-000-close handoff entry; Done When item (6) the validator green; Done When item (7) per-file handoff entries appended) |
| Task status | `Approved for apply` |
| Date last updated | `2026-05-20` |

- `Ready for proposal` means this packet is authored and awaits Human gatekeeper review. The Project Manager role cannot advance `Task status` to `Approved for apply` per CLAUDE.md; only the Human gatekeeper may flip it after reviewing this packet in a separate conversation.
- **Standard three-conversation pattern continues at T-005 per ADR-064.** Per ADR-064 (recorded in `_context/09_decision_log.md` at T-003 close and operationally re-affirmed at T-004 per the 2026-05-20T16:27:16Z PM-turn handoff entry), the T-003 inline-authorization compression was explicitly one-time and does **not** extend to T-005. T-005 follows the canonical sequence: (i) this PM-role conversation produces the packet at `Ready for proposal`; (ii) a separate Human-gatekeeper conversation reviews this packet, optionally edits, and flips `Task status` to `Approved for apply`; (iii) a third conversation invokes `/start-working` for Architect-role execution against the approved packet. The compression is not re-litigated for T-005.

## Task Objective

- Problem to solve now: `_context/13_threat_model_and_data_classification.md` does not yet exist. The framework's file 13 captures the adversaries the product is calibrated against, the data classes it stores and the controls around each, the trust boundaries the architecture defends, and the residual risks the design accepts as a deliberate trade — giving the Security agent something concrete to review against per the framework template's own preamble ("Without it, security review becomes vibes."). Prior `_context/` files (01, 02, 04, 05, 06, 09, 10) have **alluded to** the threat model (file 05's device-locked posture, no-`INTERNET` rule, optional biometric gate, no-Patient-data-in-failure-evidence rule, hard-delete + deliberate-confirmation pattern; file 06's Trust Boundaries paragraph; file 09's ADR-041 cloud-backup closure, ADR-043 plain-text Room encryption re-evaluation queued, ADR-053 no-Patient-data-in-failure-evidence; file 10's on-device-only logging contract) but the **canonical, single-source-of-truth threat-model and data-classification register** has not yet been authored. T-005 produces that register.
- Why now: closing `_context/13` is **M-000 Done When item (5)** per `_context/07` Delivery Plan And Milestones — the last design-tail file. After T-004 closed file 10 (validator exits 0; M-000 progress at `4 / 5 done-when items satisfied` per the 2026-05-20T18:40:07Z handoff entry), T-005 is the final design-tail item. Doing T-005 fifth (after T-001 file 12, T-002 file 11, T-003 file 09, T-004 file 10) honors the sequence locked in the 2026-05-17T14:23:24Z handoff entry; file 13's threat-model and trust-boundary analysis presupposes file 12's build-config locks (manifest permission set, keystore management, `allowBackup="false"` + `dataExtractionRules`), file 11's hard-delete deliberate-confirmation pattern, file 09's settled ADRs (especially ADR-041 cloud-backup, ADR-043 plain-text Room encryption queued for re-evaluation, ADR-056 deliberate-confirmation pattern), and file 10's on-device-only logging contract (ADR-067) — all now in place. T-005 also unblocks the **M-000 close handoff entry**: per ADR-039, the `_context/07 § Dependency And Blocker Register`'s five design-tail rows (files 09, 10, 11, 12, 13) are reconciled as resolved in the M-000-close handoff entry, not incrementally; T-005 close is the M-000 close and therefore the moment for that reconciliation.
- Expected user-visible or system-visible outcome: `_context/13_threat_model_and_data_classification.md` exists with all nine `##` sections from the framework template filled in. **Zero unresolved framework placeholders** (verifiable by `grep -cE '\{\{REQUIRED:|\{\{OPTIONAL:|\{\{EXAMPLE:'` returning 0). **Zero inline `// review` annotations** (verifiable by `grep -c '// review'` returning 0). Every `\{\{REQUIRED:\}\}` placeholder in the framework template (Scope And Assumptions four rows; Data Classification Confidential row five cells; Data Classification Restricted / Regulated row five cells; Trust Boundaries first row five cells; STRIDE first-row eight cells across Spoofing; Identity And Access Model four rows; Logging And Audit Requirements two rows) is replaced with a concrete value rooted in earlier `_context/` files. `\{\{OPTIONAL:\}\}` placeholders resolve to concrete content **or** the literal phrase `Not applicable` with a one-sentence reason (per CLAUDE.md's section-by-section drafting protocol). `\{\{EXAMPLE:\}\}` placeholders are either replaced with real content or removed entirely. `bash _framework/validate_framework.sh` continues to exit **0** (no regression; files 12 / 11 / 09 / 10 remain clean; no new violations introduced anywhere). A T-005-close handoff entry is appended to `_context/handoff_log.md` that also functions as the **M-000-close handoff entry** — advancing the M-000 progress meter from `4 / 5 done-when items satisfied` to `5 / 5 done-when items satisfied` (M-000 complete) and reconciling the five design-tail rows of `_context/07 § Dependency And Blocker Register` as resolved. New ADRs surfaced during T-005 execution are appended to `_context/09_decision_log.md` directly per the post-T-003 amendment convention; if the plain-text Room encryption re-evaluation per ADR-043 resolves to a substantively different decision, the superseding ADR lands in file 09 with `Status: Proposed` (not `Accepted`) per the post-T-003 supersession convention, awaiting a separate Human-gatekeeper ratification conversation.

## In Scope

- Author `_context/13_threat_model_and_data_classification.md` in this `/start-working` session (per CLAUDE.md "Contextualization Sessions: One File Per Conversation"). The file 13 framework template has **nine `##` sections** that must be drafted in original order, per CLAUDE.md's "Section-By-Section Drafting" protocol: (1) `## Scope And Assumptions`, (2) `## Data Classification`, (3) `## Trust Boundaries`, (4) `## STRIDE Threat Table`, (5) `## Abuse Cases`, (6) `## Identity And Access Model`, (7) `## Logging And Audit Requirements`, (8) `## Privacy And Regulatory Mapping`, (9) `## Open Security Questions`. The Architect role announces each section, batches the questions needed to draft that section, proposes a complete section draft, iterates with the Human gatekeeper, locks the section, and moves to the next — one section per turn cycle, never one placeholder at a time.
- Adapt the framework template's preamble heading from `## How To Fill This File` to `## How To Use This File` per the file-08 / file-12 / file-11 / file-09 / file-10 convention established at T-001 through T-004. The preamble's body content is preserved verbatim or near-verbatim from the framework template.
- **Ratify-or-supersede ADR-043 (plain-text Room encryption re-evaluation) as the single highest-priority agenda item.** Per the 2026-05-20T00:44:41Z T-003-close handoff entry's "Items deferred to later files" → "For `_context/13` (T-005)", T-005 must enumerate the adversaries the file-05 § Data Protection sub-rule (a) decision is calibrated against and validate or revise the plain-text-Room choice against them. Concrete adversary scenarios on the table per ADR-043: (i) full file-system access to an **unlocked** device (shoulder-surfer, family-member-with-access, opportunistic theft of an unlocked device); (ii) forensic recovery off discarded hardware (sold / donated / disposed device whose owner did not factory-reset); (iii) the pre-Section-3 cloud-backup exfiltration window (now closed by ADR-041 `allowBackup="false"` + `dataExtractionRules` — the threat is residual / historic but should be acknowledged in the STRIDE table); (iv) adb backup of an unlocked device with developer options enabled; (v) malware sandbox-escape from a co-installed app (low residual given Android sandbox + `exported="false"` receivers per ADR-038, but enumerated). The outcome of the re-evaluation is one of three: **(a) ratify ADR-043** — explicitly re-affirm plain-text Room with the enumerated-adversary list as the file-13 threat envelope, no new ADR needed; **(b) supersede ADR-043** with a new ADR proposing encrypted Room (e.g., SQLCipher) at `Status: Proposed`, awaiting Human gatekeeper ratification in a separate conversation per the post-T-003 supersession convention; **(c) partial supersession** — propose encrypting a subset of columns (e.g., medication-name free text) while leaving the rest plain-text, also at `Status: Proposed`. The Architect role does not silently amend file 05 § Data Protection — any supersession is recorded as a Proposed ADR in file 09 and surfaced in the T-005 handoff entry for gatekeeper review.
- **Validate the file-11 hard-delete deliberate-confirmation pattern (ADR-056) against the file-13 adversary enumeration.** If a shoulder-surfer-class adversary or family-member-with-access adversary is added to the enumeration, the deliberate-confirmation friction level (typed phrase vs. two-step tap vs. timed-hold per `_context/11`) may need re-evaluation. The Architect role does not silently amend file 11 — if the friction level should change, a Proposed ADR superseding ADR-056 lands in file 09 alongside the file-13 STRIDE row that surfaced the gap, awaiting gatekeeper ratification.
- Inherit and reference (do not re-litigate) the locked threat-model-adjacent decisions from prior `_context/` files. Specifically: (i) **Identity** — file 01 commits to single-Patient-on-single-device; there is no remote identity provider, no SSO, no SAML, no OIDC. Authentication factor is the device-OS-level biometric prompt (optional, Patient-configurable) per ADR-024 / file 05 § Authentication; there are no application-issued credentials, no application-issued tokens, no application-issued sessions. (ii) **Trust** — file 04's trust boundaries are the OS APIs (`AlarmManager`, `NotificationManager`, `BroadcastReceiver` ingress, `Room`, biometric prompt), not network boundaries. There is no API ingress, no inbound network traffic, no outbound network traffic per ADR-002 (no-`INTERNET`). The `BroadcastReceiver` `exported="false"` posture (ADR-038) plus `PendingIntent.FLAG_IMMUTABLE` posture (ADR-058) closes the in-IPC attack surface. (iii) **Data classes** — file 05 § Data Classification already names "Patient self-asserted medication and adherence data" as the only Confidential class; there is no `Restricted / Regulated` data per the file-05 / file-09 "we choose not to make this PHI" decision (ADR-046) because there is no HIPAA covered entity in the loop; file 13's `Restricted / Regulated` row resolves to `Not applicable` with a one-sentence reason citing ADR-046. The file-05 `Internal` and `Public` rows are also `Not applicable` because the app has no organization-internal data (no employee accounts, no telemetry-back-to-vendor stream) and no public data class. (iv) **Logging** — file 10's ADR-067 on-device-only logging contract (`Log.e` / `Log.w` for `Result.Err.Unexpected` only + Play Console Android Vitals as the sole remote evidence stream); no security-event log retention duration is meaningful because security events are inferred from Android Vitals crash signatures, not from in-app audit logs. (v) **Privacy / Regulatory** — file 05 § Compliance Applicability Matrix's three `Applicable` rows (CCPA / CPRA; Latin American national data-protection laws; informal Patient-facing privacy disclosure) and file 06 § F-014 hard-delete with deliberate-confirmation are the substantive satisfaction surface; T-005's Privacy And Regulatory Mapping table cites those existing surfaces rather than introducing new compliance obligations.
- **Close M-000 in this same task.** Per `_context/07 § M-000 Done When` items (5) through (8), T-005's close handoff entry is also the M-000 close handoff entry. The Architect-role conversation appends a single handoff entry that (i) closes T-005, (ii) advances the M-000 progress meter from `4 / 5` to `5 / 5 done-when items satisfied` (M-000 complete), (iii) reconciles the five design-tail rows of `_context/07 § Dependency And Blocker Register` (files 09, 10, 11, 12, 13) as resolved per ADR-039's "Touch file 07 once at M-000 close, not incrementally" rule, (iv) records the M-000 `Status` flip in `_context/07 § Milestone Register` from `Active` to `Complete`, and (v) explicitly opens M-001 reviewable scope for the next PM-turn refresh.
- (Supporting) Append a T-005-close + M-000-close handoff entry to `_context/handoff_log.md` per the CLAUDE.md handoff-entry template. The handoff entry surfaces any new ADRs (specifically: the device-locked threat envelope as a single ADR if the enumeration crystallizes one; the ADR-043 ratification or supersession outcome; the ADR-056 deliberate-confirmation friction-level re-affirmation or supersession outcome; the file-13 → file-05 / file-06 cross-reference convention as an ADR if the Architect role establishes one; the M-000 close as an ADR-class delivery event) and lands them as new ADRs in `_context/09_decision_log.md` at task close. Per the post-T-003 convention, **new ADRs are added directly to file 09 — not queued in handoff entries for future backfill**. Supersession ADRs (if any) land at `Status: Proposed` not `Accepted`.
- (Supporting) Edit this `_context/08_active_task_packet.md` to flip `Task status` from `Ready for proposal` to `Approved for apply` in the Human-gatekeeper-review conversation (the second of the three conversations in the standard pattern). The Architect-role conversation reads the approved packet; it does not edit the packet's `Task status` field. If the Architect-role conversation discovers a scope gap that requires the packet to be refreshed, the discovery is surfaced in a mid-session handoff entry and routed back to a fresh PM-refresh conversation — not silently edited.

## Explicitly Out Of Scope

- Contextualizing any other framework file in this same conversation. Per CLAUDE.md's "Contextualization Sessions: One File Per Conversation" rule, T-005 covers `_context/13_threat_model_and_data_classification.md` **only**. The other agreed optional files (14 agent roles, 15 glossary) remain explicitly out of project scope per the file-07 design-tail enumeration; if either later becomes desirable, that is a fresh task packet in a future PM-refresh conversation, not part of T-005. Per ADR-064, the T-003 inline-authorization compression does **not** extend to T-005 — three conversations.
- Writing any code, configuration file, build artifact, or `_source/` / `_build/` artifact. T-005 is design-phase work — the current delivery phase is `MVP — design (pre-code)` per `_context/07 § Delivery Summary`, and CLAUDE.md's Architect role contract explicitly says the Architect "may not write to `_source/`". T-005 records the threat model the system will be built against; it does not introduce any test code, CI config, or build-config artifact that would crystallize those threats. The future Apply-Mode milestones (M-001 onward) consume the file-13 threat-model surface as verification input (notably M-001's architecture-conformance Konsist rules, M-006's hard-delete F-014 test, the Security-role guardrail review at every task close) but T-005 itself produces only the documentation surface.
- Re-litigating or amending any settled security-adjacent decision from prior `_context/` files **except ADR-043 and (conditionally) ADR-056**. The locked decisions enumerated under Section 4 In Scope (file 01 single-Patient framing, file 04 trust-boundary surface, file 05 no-`INTERNET` / optional biometric / no-Patient-data-in-failure-evidence / hard-delete-only / no-`HIPAA` posture, file 06 F-014 + BR-010 / BR-011 windows, file 09 ADR-041 cloud-backup closure / ADR-053 failure-evidence rule / ADR-038 exported=false / ADR-058 FLAG_IMMUTABLE / ADR-046 not-PHI, file 10 ADR-067 on-device-only logging, file 11 deliberate-confirmation tokens, file 12 keystore + dataExtractionRules) are inputs to T-005, not re-openable subjects. **Two exceptions:** ADR-043 (plain-text Room encryption) is explicitly queued for re-evaluation per the T-003 close handoff entry, with three permitted outcomes (ratify / supersede with full encryption / supersede with partial column encryption) — any supersession lands as a Proposed ADR in file 09; ADR-056 (deliberate-confirmation friction level) is conditionally re-openable if the file-13 adversary enumeration adds a class that justifies a friction-level revision, also as a Proposed ADR. If during T-005 execution the Architect role discovers that some other section cannot be drafted without contradicting an additional locked decision, the contradiction is surfaced in the T-005-close handoff entry as an "Items needing follow-up" item routed to a future PM-refreshed task packet — not silently resolved against the prior file.
- Authoring threat-model content for any system surface that NemoPill does not yet have. Specifically: no STRIDE rows for a backend service (there is no backend per ADR-002); no STRIDE rows for a public API ingress (there is no API surface); no Trust Boundary rows for SSO / SAML / OIDC (no auth surface beyond the device-OS-level biometric gate); no Trust Boundary rows for inter-microservice mTLS (no microservices); no Privacy And Regulatory Mapping rows for HIPAA (not a covered entity per ADR-046); no Logging And Audit Requirements rows for centralized SIEM (no SIEM, no log shipping per ADR-002 / ADR-067). Sections that have no substantive content for NemoPill resolve to `Not applicable` with a one-sentence reason rooted in the relevant prior `_context/` file (e.g., "Identity provider → `Not applicable` — NemoPill issues no application-level identities per `_context/01 § Distribution Scope` and `_context/05 § Authentication`; the only authenticated event is an optional device-OS biometric prompt with no application-issued credential.").
- Editing the framework's `_framework/13_threat_model_and_data_classification.md` template. CLAUDE.md treats `_framework/` as read-only template content. T-005 reads the framework template to inherit structure and writes the contextualized output to `_context/13_threat_model_and_data_classification.md` only.
- Amending file 09 with ADRs that record T-005-introduced decisions during the **PM-refresh** conversation (this conversation). The PM-refresh produces no new ADRs — its output is this packet. Any ADRs surfaced by T-005 execution land in file 09 during the Architect-role conversation, after the Human gatekeeper has flipped `Task status` to `Approved for apply`. The T-005 handoff entry at the end of the Architect-role conversation enumerates the ADRs that landed.
- Reconciling `_context/07 § Dependency And Blocker Register` incrementally. Per ADR-039, file 07 is touched **once** at M-000 close — not at each design-tail-file close. T-005 close is the M-000 close, so file 07 reconciliation is in scope **for the Architect-role execution conversation**, not for any earlier conversation in the three-conversation sequence. The PM-refresh conversation (this one) does not touch file 07; the Architect-role conversation reconciles file 07 as part of the same handoff entry that closes T-005 and M-000.

## Acceptance Criteria

- `AC-001` — File exists and is framework-valid. `_context/13_threat_model_and_data_classification.md` exists with: (i) zero `\{\{REQUIRED:\}\}` / `\{\{OPTIONAL:\}\}` / `\{\{EXAMPLE:\}\}` markers (verifiable by `grep -cE '\{\{REQUIRED:|\{\{OPTIONAL:|\{\{EXAMPLE:'` returning 0); (ii) zero inline `// review` annotations (verifiable by `grep -c '// review'` returning 0); (iii) all nine `##` section headings present in original framework-template order — `## Scope And Assumptions`, `## Data Classification`, `## Trust Boundaries`, `## STRIDE Threat Table`, `## Abuse Cases`, `## Identity And Access Model`, `## Logging And Audit Requirements`, `## Privacy And Regulatory Mapping`, `## Open Security Questions` — plus the preamble `## How To Use This File` adapted from the framework template's `## How To Fill This File` per the file-08 / file-12 / file-11 / file-09 / file-10 convention; (iv) `bash _framework/validate_framework.sh` exits **0** (no regression from T-004's clean baseline; the validator's `Framework ready for Claude handoff.` output is preserved).
- `AC-002` — Threat and classification content is concrete, scored, and rooted in prior `_context/` files. Every STRIDE row carries a numeric `Likelihood` (1-5), numeric `Impact` (1-5), concrete `Existing Mitigation` (citing the originating `_context/` file or the literal phrase "none yet" with a one-sentence rationale), and a `Residual Risk` rating of `low` / `medium` / `high` — no row may be empty or adjective-only. Every Data Classification row is either populated with concrete cells (Examples / Storage Location / Access Control / Retention / Deletion Trigger) or resolves to `Not applicable` with a one-sentence reason citing the originating `_context/` file. Every Trust Boundaries row carries a concrete `What Crosses` / `Validation Owner` / `Authentication Mechanism` or resolves to `Not applicable` with a one-sentence reason. The `Identity And Access Model` four required fields are either populated or `Not applicable`-with-reason — adjective-only entries ("strong", "robust", "secure") are explicitly rejected. The STRIDE table includes at least one concrete row per category that is **applicable to NemoPill** (Spoofing, Tampering, Information Disclosure, Denial Of Service, Elevation Of Privilege are expected applicable for the device-locked threat model; Repudiation is allowed to resolve to `Not applicable` with a one-sentence reason given there is no multi-actor audit-trail requirement per `_context/01` single-Patient framing — but the Architect role must explicitly take that position rather than leave the row empty).
- `AC-003` — Cross-file consistency. The authored threat-model register is consistent with the contextualized content of `_context/01-07` and `_context/09-12`. Specifically: (i) Ubiquitous Language terms used in threat-model text match the canonical terms in `_context/01` (Patient, Reminder, Dose, Confirmation, Adherence, Schedule, Medication — not User, Account, Notification, Pill, Mark, Compliance, etc.); (ii) Bounded Context names used in trust-boundary or STRIDE rows match the canonical names from `_context/01` (Medication Management, Scheduling & Reminders, Adherence Tracking) referenced via the file-04 module names (`:medication-management`, `:scheduling`, `:notifications`, `:adherence-tracking`); (iii) Mitigation citations match `_context/05` § Data Protection sub-rules, `_context/05` § Authentication, `_context/06` § F-014 hard-delete, `_context/09` § ADR-002 (no-`INTERNET`) / ADR-038 (exported=false) / ADR-041 (cloud-backup closure) / ADR-053 (no-Patient-data-in-failure-evidence) / ADR-058 (FLAG_IMMUTABLE) / ADR-067 (on-device-only logging) verbatim — no paraphrased mitigation strings; (iv) Data Classification rows that resolve to `Not applicable` cite ADR-046 (not-PHI) by ID rather than re-stating the rationale; (v) Compliance citations in `## Privacy And Regulatory Mapping` match the three `Applicable` rows in `_context/05 § Compliance Applicability Matrix` (CCPA / CPRA; Latin American national data-protection laws; informal Patient-facing privacy disclosure) and reference `legal/PRIVACY_POLICY.en.md` + `.es.md` as the substantive satisfaction artifact per ADR-021; (vi) Any ADR references in section bodies (where decisions T-005 locks correspond to a future ADR) use the file-09 ADR-ID format (e.g., `ADR-068`, `ADR-069`, …) assigned in chronological order from the current top of file 09 (`ADR-067`); (vii) The ADR-043 ratification or supersession outcome is explicitly stated in `## Open Security Questions` (or the relevant STRIDE row's `Existing Mitigation` cell) with the file-09 ADR-ID of the new Proposed ADR if a supersession lands; (viii) Any new Proposed ADRs (e.g., a supersession of ADR-043) land in file 09 with `Status: Proposed` and the Architect-role handoff entry surfaces them for gatekeeper ratification — not as `Accepted`.
- `AC-004` — Handoff entry appended, file 09 amended, M-000 closed, file 07 reconciled. A T-005-close + M-000-close handoff entry is appended to `_context/handoff_log.md` following CLAUDE.md's handoff-entry template. The entry advances the M-000 progress line from `4 / 5 done-when items satisfied` to `5 / 5 done-when items satisfied` (files 12, 11, 09, 10, 13 closed). The entry enumerates the ADRs T-005 added to `_context/09_decision_log.md` (each new ADR listed with its ADR-ID, title, status — `Accepted` or `Proposed` — and one-line decision summary). The entry includes a Validator result line (`Framework ready for Claude handoff.` — the validator remains clean), a Next role line (Project Manager — refresh packet to T-006 against the first M-001 task slice per `_context/07 § M-001 Done When`), and a Per-CLAUDE.md-one-file-per-conversation closing line (end this conversation; T-006 PM refresh begins in a new conversation). **`_context/07` is also updated in the same Architect-role turn**: the five design-tail rows of `§ Dependency And Blocker Register` reconciled as resolved per ADR-039; the M-000 row of `§ Milestone Register` flipped from `Active` to `Complete`; the M-001 row's `Prerequisites` cell already names "`M-000` complete" so no edit there.

## Tests To Add First

- **Unit / integration / contract / UI tests in the framework-template sense: not applicable for T-005.** T-005 is an Architect-role documentation task whose deliverable is `_context/13_threat_model_and_data_classification.md`; it writes no code in `_source/`, per CLAUDE.md's Architect role contract. The framework template's `## Tests To Add First` placeholders, which specify *"unit, integration, contract, or UI test"* and *"failure-path or regression test"*, presuppose a code deliverable, which T-005 does not have. This resolution is the convention established at T-001, continued at T-002 / T-003 / T-004, and ratified as ADR in file 09 as of T-003 close for every Architect-role documentation task packet.
- **Verification surface for T-005 lives in Section 6 `## Acceptance Criteria`, not here.** Concretely: `AC-001`'s executable `grep -cE '\{\{REQUIRED:|\{\{OPTIONAL:|\{\{EXAMPLE:'` and `grep -c '// review'` commands plus the `bash _framework/validate_framework.sh` exit-code check; `AC-002`'s numeric-Likelihood / numeric-Impact / non-empty-Existing-Mitigation rule applied to every STRIDE row plus the Data Classification / Trust Boundaries / Identity rules; `AC-003`'s cross-file consistency cross-walk against the eight enumerated alignment hooks (Ubiquitous Language; Bounded Context naming; mitigation citations to ADR IDs; ADR-046 Not Applicable resolutions; ADR-021 Privacy Policy citations; new ADR-ID format; ADR-043 outcome surfaced; Proposed-vs-Accepted ADR status); `AC-004`'s handoff-entry completeness, file-09 amendment, M-000 close, and file-07 reconciliation.
- **Convention recorded as ADR in file 09 as of T-003 close (no new ADR needed at T-005).** T-003 already landed the ADR that records `## Tests To Add First → Not applicable for Architect-role documentation task packets; the verification surface lives in ## Acceptance Criteria instead`. T-005 honors that ADR without re-stating it. The post-T-003 file-09 convention is: pre-existing ADRs do not need to be re-affirmed in each new packet.

## Likely Change Surface

- Files or modules likely affected: (i) `_context/13_threat_model_and_data_classification.md` — created as the primary deliverable from `_framework/13_threat_model_and_data_classification.md`; (ii) `_context/09_decision_log.md` — amended with any new ADRs T-005 surfaces (the device-locked-threat-envelope ADR if it crystallizes; the ADR-043 ratification or supersession; the ADR-056 friction-level re-affirmation or supersession; any others), with supersession ADRs at `Status: Proposed` per the post-T-003 supersession convention; (iii) `_context/handoff_log.md` — appended with the T-005-close + M-000-close handoff entry per Section 6 `AC-004`; (iv) `_context/07_delivery_plan_and_milestones.md` — edited at M-000 close per ADR-039: the five design-tail rows of `§ Dependency And Blocker Register` reconciled as resolved; the M-000 row of `§ Milestone Register` flipped `Status` from `Active` to `Complete`; this is the first edit to file 07 since its T-001-precedent contextualization at 2026-05-17T14:23:24Z and the only edit T-005 is authorized to make; (v) `_context/08_active_task_packet.md` — this file, edited once during the Human-gatekeeper-review conversation to flip `Task status` from `Ready for proposal` to `Approved for apply` (then edited again at T-005 close by the next PM-refresh conversation to advance the packet to T-006). The Architect-role conversation does **not** edit this file's `Task status`.
- Files or modules that must remain untouched: (i) `_source/` in its entirety — CLAUDE.md's Architect role contract forbids `_source/` mutation regardless of `Task status`; (ii) `_build/` in its entirety; (iii) every already-contextualized `_context/` file except those listed above — files 01, 02, 03, 04, 05, 06, 10, 11, 12 are out of T-005's authority to revise; **specifically, file 05's § Data Protection sub-rule (a) is NOT edited even if ADR-043 is superseded** — the supersession is recorded as a Proposed ADR in file 09; file 05 is amended only after gatekeeper ratification in a separate conversation; (iv) `_framework/` in its entirety — read-only template content; (v) `CLAUDE.md` itself — the framework's instruction surface is out of T-005's authority to revise.
- Contracts or schemas that may change: no runtime contracts are changed by T-005. There are no API endpoints to alter, no event payloads to evolve, no UI contracts to revise. The Room database schema is **conditionally affected** if ADR-043 is superseded toward encrypted-Room — but the supersession lands at `Status: Proposed` only, not `Accepted`; the schema migration is M-001 / M-003 implementation work, not T-005 documentation work. T-005 *records* the threat envelope against contracts that prior sessions already locked — the OS-API trust boundaries, the no-`INTERNET` posture, the optional biometric gate, the `exported="false"` receivers, the `FLAG_IMMUTABLE` PendingIntents, the bilingual Privacy Policy, the on-device-only logging contract — and crystallizes them as adversary-scored mitigations. These are *records* of threat-and-mitigation pairs that the implementation will be tested against (M-001 onward); they cross the runtime-contract threshold only when the corresponding tests are written, not at T-005.

## Dependencies, Risks, And Blockers

- Dependencies: (i) `_context/` files 01 through 12 already contextualized and validator-clean (confirmed at T-004 close — `bash _framework/validate_framework.sh` exits 0 with `Framework ready for Claude handoff.`); without those files locked, T-005's threat-model register would have no source-of-truth to root its mitigations in. (ii) `_context/09_decision_log.md` already contains the 67 backfilled-plus-T-004 ADRs (ADR-001 through ADR-067), so T-005 has a settled decision surface to cite from and to extend with new ADRs at task close. (iii) Human gatekeeper (Isidro) review-and-approval bandwidth for the standard three-conversation pattern per the `_context/07 § Delivery Summary` ~3-4 hrs/week baseline; the three conversations may span multiple days, which is acceptable. (iv) CLAUDE.md instruction surface unchanged across the three conversations. (v) The post-T-003 file-09 amendment convention (new ADRs land in file 09 directly at task close; supersession ADRs land at `Status: Proposed`) is established and operational, exercised twice at T-004 close (three new ADRs landed at `Accepted`). (vi) ADR-039's "Touch file 07 once at M-000 close" rule is the operational instruction for the file-07 reconciliation step that T-005 close executes.
- Risks: (i) **ADR-043 re-evaluation scope-creep risk.** The plain-text Room encryption re-evaluation is a substantive security decision that may pull the Architect-role conversation into deep technical analysis (SQLCipher integration cost, key-derivation choice between Android Keystore-backed master key vs. user-derived passphrase, per-column encryption vs. whole-database, performance impact on Adherence calculation queries, migration cost for existing-Patient data) that consumes the conversation's budget before all nine sections are drafted. *Mitigation:* the section ordering is locked (Scope And Assumptions first → Data Classification second → … → Open Security Questions last); ADR-043 re-evaluation happens during section 4 STRIDE Threat Table (the Information Disclosure row is the natural surface) and section 9 Open Security Questions (where the outcome lands); if the re-evaluation cannot be resolved within the section's conversation budget, it lands in section 9 as an open question with a Proposed-ADR superseding ADR-043, deferring the substance to a future gatekeeper-ratification conversation rather than blocking T-005 close. (ii) **Adjective-mitigation risk.** The framework template's `## How To Fill This File` does not explicitly warn against adjective-only mitigations the way file 10's preamble does for NFRs, but the same anti-pattern applies — STRIDE rows whose `Existing Mitigation` cell says "input validation" or "least privilege" without citing a concrete `_context/` file or ADR ID fail to satisfy AC-002. *Mitigation:* AC-002 mandates citation of the originating `_context/` file or ADR ID for every Existing Mitigation cell; "none yet" is acceptable but adjective-only is not. The Human gatekeeper rejects adjective-only rows at section-acceptance time during the Architect-role conversation. (iii) **STRIDE-category-collapse risk.** Several STRIDE categories may collapse to mostly `Not applicable` or to single-row entries for NemoPill (notably Repudiation — no multi-actor audit-trail need per single-Patient framing; Denial Of Service in the network sense — no network surface to flood). The risk is that the resulting table looks thin and feels like the file hasn't been substantively contextualized. *Mitigation:* each collapsed category carries an explicit `Not applicable` row with a one-sentence reason citing the originating `_context/` file (the same convention used at T-004 for file 10's Scalability And Capacity section); the substantive STRIDE work concentrates in the categories that **are** load-bearing for NemoPill — Spoofing (unauthorized device access), Tampering (storage modification by malware sandbox-escape or adb), Information Disclosure (the ADR-043 surface; shoulder-surfer of notifications on the lock screen; forensic recovery off discarded hardware), Denial Of Service in the local sense (alarm spam, notification overflow, Doze-induced reliability failure), Elevation Of Privilege (the optional-biometric-bypass surface; a co-installed app exploiting an unprotected `BroadcastReceiver` — closed by ADR-038 but enumerated). (iv) **Cross-file drift risk.** T-005 introduces nine new sections that reference content from all twelve prior `_context/` files plus ~12-15 file-09 ADRs by ID; the risk is that a mitigation citation drifts from the source file (e.g., quoting `exported="false"` as ADR-039 when it is actually ADR-038, or quoting the late-Reminder window as 90 minutes when file 06 says 60 minutes). *Mitigation:* AC-003's eight alignment hooks are the post-execution verification; mid-execution, the Architect role spot-checks every ADR-ID citation against the file-09 entry before locking the section. (v) **Wall-clock-span risk.** The standard three-conversation pattern spans the PM-refresh (this conversation) → gatekeeper-review → Architect-execution sequence, which may take days at 3-4 hrs/week gatekeeper bandwidth. The risk is that CLAUDE.md or some prior `_context/` file is edited between conversations, invalidating an assumption in this packet. *Mitigation:* the Architect-role conversation begins by reading `_context/handoff_log.md` for any entries timestamped after this packet's `Date last updated` (2026-05-20) and re-runs the framework validator before executing; if any drift is detected, the Architect surfaces it in a mid-session handoff entry and routes back to a PM-refresh conversation rather than executing under stale assumptions. (vi) **M-000-close housekeeping risk.** T-005 close is the M-000 close, which includes the file-07 § Dependency And Blocker Register reconciliation and the § Milestone Register `Status` flip — work that touches a previously-locked file. The risk is that the Architect role either forgets the file-07 reconciliation (in which case M-000 is not actually closed) or executes it imprecisely (in which case the file-07 wording drifts). *Mitigation:* AC-004 names the file-07 reconciliation steps explicitly and tightly (five design-tail rows reconciled as resolved; M-000 row `Status` flipped from `Active` to `Complete`); the Architect-role handoff entry must show the file-07 diff (or equivalent prose summary) as part of the close evidence.
- Blockers: none. All authoring-time prerequisites (files 01-12 contextualized, framework validator clean, file 09 contains ADR-001 through ADR-067, CLAUDE.md unchanged at packet-authoring time, T-004-close handoff entry complete and timestamped, post-T-003 file-09 convention operational and exercised, ADR-039 file-07-touch rule operational) are in place. T-005 proceeds to Human gatekeeper review at this packet's `Ready for proposal` status; once flipped to `Approved for apply` in a separate conversation, the Architect-role `/start-working` conversation may execute.

## Rollback Or Recovery Notes

- Rollback: if T-005 execution fails — produced file is structurally wrong, a STRIDE-row mitigation contradicts a prior locked decision, the validator regresses, the new ADRs introduce conflicts with prior ADRs in file 09, the ADR-043 re-evaluation lands an incoherent supersession, the file-07 reconciliation drifts the milestone register state, the conversation budget exhausts before all nine sections are drafted — and the Human gatekeeper decides to abandon the produced file 13, rollback is one `git restore _context/13_threat_model_and_data_classification.md _context/09_decision_log.md _context/handoff_log.md _context/07_delivery_plan_and_milestones.md _context/08_active_task_packet.md` away. There is no `_source/` state to revert, no runtime-state cleanup, no deployment to undo. The Human-gatekeeper-review flip from `Ready for proposal` to `Approved for apply` is rolled back by the same `git restore` invocation; a re-run begins with a fresh PM refresh against the restored T-004 packet, which would itself be re-refreshed to T-005 at the start of the re-run PM-refresh conversation. The file-07 reconciliation is part of the same atomic restore — file 07 returns to its pre-T-005 state with M-000 still `Active` and the five design-tail rows still listed.
- Recovery from partial implementation: the most likely partial-implementation failure mode is conversation-context exhaustion during the Architect-role conversation before all nine sections are drafted, or before the file-07 reconciliation completes. Recovery has two paths: **(a) resume in a new conversation** — the new `/start-working` invocation will see `_context/13_threat_model_and_data_classification.md` already partially present (containing the structural preamble plus N of 9 sections drafted), run the framework validator to confirm the file's existence and identify which sections still contain unresolved placeholders, read the mid-session handoff entry in `_context/handoff_log.md` to inherit context, and continue the section sequence from the next undrafted section per CLAUDE.md's per-file resume protocol. The packet's `Approved for apply` flag (set by the gatekeeper-review conversation before the Architect's first conversation began) remains set, so the resume conversation can proceed directly to Apply Mode without re-litigating gatekeeper approval; the resume is a continuation of the same T-005 task, not a new task. The file-07 reconciliation is deferred to the *final* T-005 close (the conversation that drafts the last section), not split across resume conversations. **(b) full restart** — `git restore _context/13_threat_model_and_data_classification.md`, abandon the partial sections, restart fresh in a new conversation, possibly with revised section-ordering or detail-level choices if the partial draft revealed that the original choices were ill-calibrated. Path (a) is preferred unless the partial draft has structural problems that surgery cannot fix. The mid-session checkpoint convention from T-002 / T-003 / T-004 (mid-session handoff entry after every ~3-4 sections) supports path (a) recovery and is especially important for T-005 given the nine-section length.

## Handoff Rules For The Agent

- Proposal Mode is the default for T-005 *authoring* (the PM-role pass that produced this packet — already complete as of this packet's commit); Apply Mode is **not yet authorized** for T-005 *execution*. Apply Mode for T-005 requires the Human gatekeeper to flip this packet's `Task status` from `Ready for proposal` to `Approved for apply` in a separate review conversation. The Architect-role `/start-working` conversation reads the `Task status` field; if it is not `Approved for apply`, the Architect role refuses to write `_context/13_threat_model_and_data_classification.md` and instead surfaces the missing gatekeeper flip to the Human in chat. Per ADR-064, the T-003 inline-authorization compression does **not** transfer to T-005 — the gatekeeper review must happen in its own conversation.
- The agent must respect the locked decisions from prior `_context/` files as inputs, not as re-openable subjects, **except for ADR-043 (plain-text Room encryption) which is explicitly queued for ratify-or-supersede re-evaluation, and ADR-056 (deliberate-confirmation friction level) which is conditionally re-openable**. The exhaustive list of locked threat-model-adjacent inputs is enumerated in Section 4 In Scope (file 01 single-Patient framing; file 04 trust-boundary surface; file 05 no-`INTERNET`, optional biometric, no-Patient-data-in-failure-evidence, hard-delete, not-PHI per ADR-046; file 06 F-014 + BR-010 / BR-011; file 09 ADRs by ID; file 10 ADR-067; file 11 deliberate-confirmation tokens; file 12 keystore + dataExtractionRules). If during T-005 execution the Architect role identifies a load-bearing locked input not in this list, the discovery is recorded in the T-005 handoff entry's "Items needing follow-up" and the packet is treated as authoritative for what is in scope. Any supersession of ADR-043 or ADR-056 lands as a Proposed ADR (not Accepted) in file 09 per the post-T-003 supersession convention.
- The agent must design verification first (Section 6 `## Acceptance Criteria`), implementation second (the nine `##` sections drafted in original order per CLAUDE.md's section-by-section drafting protocol), and produce a handoff entry on completion that also closes M-000 and reconciles file 07. Per the file-08 / file-12 / file-11 / file-09 / file-10 convention ratified as a file-09 ADR, the `## Tests To Add First` section resolves to `Not applicable` for this Architect-role documentation task and the verification surface lives in `## Acceptance Criteria` instead.
- The agent must follow the section-by-section drafting protocol strictly. CLAUDE.md's "Section-By-Section Drafting" rule overrides the placeholder-by-placeholder procedure in `start-working/SKILL.md`. For each of the nine file-13 sections, the Architect announces the section, batches the questions needed to draft it, proposes a complete section draft (all placeholders resolved, all tables filled, all rows either concrete-with-citation or `Not applicable`-with-reason), iterates with the Human gatekeeper, locks the section, and only then moves to the next. The file is not written to `_context/13_threat_model_and_data_classification.md` until all nine sections are accepted; once written, the framework validator is run to confirm AC-001(iv), the new ADRs are appended to `_context/09_decision_log.md` to satisfy AC-004 (with supersession ADRs at `Status: Proposed`), the file-07 reconciliation is executed (five design-tail rows resolved, M-000 `Status` flipped to `Complete`), and the T-005 + M-000-close handoff entry is appended to `_context/handoff_log.md` to close the task and the milestone.
- If the task packet conflicts with another document discovered during execution (e.g., a new edit to `_context/05` during the wall-clock span of the three conversations, an ADR in file 09 that contradicts a section-draft assumption), the agent must surface the conflict in the T-005-close handoff entry as an "Items needing follow-up" item routed to a future PM-refreshed task packet — not silently resolve it. The agent must re-run `bash _framework/validate_framework.sh` at the start of the Architect-role conversation to detect any inter-conversation drift before beginning to draft sections.
